"""Agentes de vídeo do IA-Linux MiniVideo.

``devices`` (CPU, dGPU/APU Vulkan, CUDA) → ``registry`` (agentes e
roteamento) → ``planner`` (pedido → etapas, explicado antes de rodar) →
``executor`` (comandos locais sob o Safety Guard).
"""
