[package]
name = "ai-core"
version = "0.7.0"
edition = "2021"
description = "IA Linux Minimal — daemon central de hardware, modelos, backend e agentes"
license = "Apache-2.0"

[[bin]]
name = "ai-core"
path = "src/main.rs"

[dependencies]

[profile.release]
opt-level = "s"
lto = true
strip = true
panic = "abort"
