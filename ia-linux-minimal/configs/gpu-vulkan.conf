# Perfil de exemplo — GPU AMD com Vulkan/RADV disponível (ex.: RX 580).
# Threads ficam moderadas porque grande parte do trabalho vai para a GPU
# (ver ia-gpu mode vulkan / BACKEND SET vulkan); contexto maior porque a
# VRAM/offload permite. Ajuste conforme sua VRAM real.

IA_THREADS=6
IA_CTX=8192
IA_BATCH=512
