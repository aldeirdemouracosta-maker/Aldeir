# Perfil de exemplo — hardware modesto (4-8 GB RAM, sem GPU compatível).
# Copie para /data/config/runtime.conf no sistema instalado, ou ajuste os
# valores calculados automaticamente por ai-core (ver
# ai-core/src/hardware.rs, Profile::Tiny/Low). Formato: CHAVE=valor,
# '#' inicia comentário — parser em ai-core/src/config.rs.

IA_THREADS=3
IA_CTX=1024
IA_BATCH=128
