# Fragmento base de configuração do kernel — IA Linux Minimal
# Linux 6.18.52 LTS. Aplicado a QEMU e a alvos físicos; particularidades de
# hardware físico (AMDGPU, NVMe, USB) ficam em physical.fragment.
#
# Uso: BR2_LINUX_KERNEL_USE_CUSTOM_CONFIG + BR2_LINUX_KERNEL_CONFIG_FRAGMENT_FILES
# (ver buildroot/configs/*_defconfig). Este arquivo não é um .config completo;
# é a base sobre a qual os fragments e `make olddefconfig` completam o resto.

# Console e init
CONFIG_TTY=y
CONFIG_SERIAL_8250=y
CONFIG_SERIAL_8250_CONSOLE=y
CONFIG_VT=y
CONFIG_VT_CONSOLE=y
CONFIG_TMPFS=y
CONFIG_DEVTMPFS=y
CONFIG_DEVTMPFS_MOUNT=y

# cgroups v2 — AI Resource Manager (isolamento de agentes/tarefas).
# CONFIG_MEMCG é o que sustenta memory.low/memory.min (usado desde a
# 0.6 por ai-core/src/memory.rs para proteger a memória do modelo ativo/
# KV cache contra reclaim — ver docs/architecture.md). A mesma
# implementação de memcg do kernel serve tanto cgroup v1 quanto v2; não
# existe um CONFIG_ separado para "v2" — o que muda é a hierarquia
# montada em runtime (unificada = v2), não a configuração do kernel.
CONFIG_CGROUPS=y
CONFIG_CGROUP_SCHED=y
CONFIG_CGROUP_CPUACCT=y
CONFIG_CGROUP_FREEZER=y
CONFIG_CGROUP_DEVICE=y
CONFIG_MEMCG=y
CONFIG_CGROUP_PIDS=y
CONFIG_CGROUP_BPF=y

# eBPF / sched_ext — base para o futuro AI Scheduler (etapa 0.7)
CONFIG_BPF=y
CONFIG_BPF_SYSCALL=y
CONFIG_BPF_JIT=y
CONFIG_HAVE_EBPF_JIT=y
CONFIG_BPF_EVENTS=y
# CONFIG_SCHED_CLASS_EXT is not set
# desligado por padrão nesta etapa; habilitado explicitamente pelo fragmento
# kernel/config/sched-ext.fragment quando o AI Scheduler experimental for
# ligado (0.7). Ver kernel/patches/README.md.

# DAMON — AI Memory (etapa 0.6, implementada: ver ai-core/src/memory.rs).
# CONFIG_DAMON_RECLAIM habilita o módulo cujos parâmetros ai-core lê/
# escreve em /sys/module/damon_reclaim/parameters (enabled, min_age,
# quota_ms, quota_sz). CONFIG_DAMON_SYSFS fica ligado para uso futuro
# (a hierarquia completa de kdamonds/contexts/schemes não é usada nesta
# versão — ver a nota de escopo em memory.rs).
CONFIG_DAMON=y
CONFIG_DAMON_VADDR=y
CONFIG_DAMON_PADDR=y
CONFIG_DAMON_SYSFS=y
CONFIG_DAMON_RECLAIM=y

# zram — swap comprimido para hardware com pouca RAM
CONFIG_ZRAM=y
CONFIG_ZSMALLOC=y
CONFIG_CRYPTO_LZ4=y
CONFIG_CRYPTO_ZSTD=y

# Filesystems
CONFIG_EXT4_FS=y
CONFIG_VFAT_FS=y
CONFIG_TMPFS_POSIX_ACL=y
CONFIG_EROFS_FS=y
# EROFS reservado para a futura partição SYSTEM somente-leitura (0.1B+)

# rede básica (llama-server local, sincronização futura de modelos)
CONFIG_NET=y
CONFIG_INET=y
CONFIG_UNIX=y
CONFIG_PACKET=y

# módulos
CONFIG_MODULES=y
CONFIG_MODULE_UNLOAD=y
